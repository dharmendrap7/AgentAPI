export enum userType {
    ADMIN = 'Admin',
    TEACHER = 'Teacher',
    PARENT = 'Parent'
}

export enum priorityType {
    LOW = 'Low',
    MEDIUM = 'Medium',
    HEIGH = 'Heigh'
}

export enum eventType {
    DAILY_ROSTER = "Daily Roster",
    MEETING = "Meeting"
}